[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.pool"

[deploy]
startCommand = "python -m uvicorn pool_server:app --host 0.0.0.0 --port $PORT --no-access-log --log-level info"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
